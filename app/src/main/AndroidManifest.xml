<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dzikirqu.smk.com.jogjaunity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WisataActivity"
            android:theme="@style/Wisata" />
        <activity
            android:name=".KulinerActivity"
            android:theme="@style/Kuliner" />
        <activity
            android:name=".BioskopActivity"
            android:theme="@style/Bioskop" />
        <activity android:name=".OlehActivity"></activity>
    </application>

</manifest>